{
  "image": "mcr.microsoft.com/devcontainers/universal",
  "hostRequirements": {
    "cpus": 4
  },
  "waitFor": "onCreateCommand",
  "updateContentCommand": "python3 -m pip install -r requirements.txt; python3 -m pip install keras-visualizer; python3 -m pip install tensorflow; sudo apt update; sudo apt upgrade -y; sudo apt install graphviz -y",
  "postCreateCommand": "",
  "customizations": {
    "codespaces": {
      "openFiles": []
    },
    "vscode": {
      "extensions": [
        "ms-toolsai.jupyter",
        "ms-python.python"
      ]
    }
  }
}
